

allprojects {
    apply plugin: 'idea'
    group = 'solutions.fluidity'
    version = '1.1'
    ext.isReleaseVersion = !version.endsWith("SNAPSHOT")
}

subprojects {
    apply plugin: 'java'
    repositories {
        mavenCentral()
    }
}
